#include "main.h"
#include <stdio.h>

/**
 * mul -  function that multiplies two integers.
 * @a: first integer
 * @b: second integer
 *
 * Return: product of a*b
 */

int mul(int a, int b)
{
	return (a * b);
}