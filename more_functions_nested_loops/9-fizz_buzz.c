#include <stdio.h>
/**
* main - prints the numbers from 1 to 100
*
* Return : == 0 (succefuly)
*/

int main(void)
{
	int i = 1;

	for (i = 1; i <= 100; i++)
	{
		if (i % 3 == 0 && i % 5 == 0)
		printf("FizzBuzz");

		if else(i % 3 == 0)
		printf("Fizz")

		else
	}
	putchar('/n');
	return (O);
}
